workflow:
  autoTrigger: true
  branchFilters:
    - master
    - develop
    - /^fb-/
  containers:
    - container:
      name: dummy-alpine
      image: alpine
      command: |
        ls /
        sleep 20